import React from "react";

const Button = ({ title }) => {
  return (
    <button className="bg-[#5046E4] px-4 py-2 text-white rounded-md font-semibold">{title}</button>
  );
};

export default Button;
